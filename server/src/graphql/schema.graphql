type Book {
  title: String
  author: Author
}

type Author {
  name: String
  birthdate: String
  books: [Book]
  location: String
}

type Query {
  books: [Book]
}

type Query {
  authors: [Author]
}
